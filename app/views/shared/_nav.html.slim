- opts ||= opts
- white_nav ||= @white_nav

nav.navbar.navbar-expand-lg id="#{opts[:id] if opts}" class="#{'white-nav' if white_nav} #{opts[:class] if opts}"
  .container

    = link_to root_path, class: "navbar-brand" do
      = image_tag 'jelly.png'

    button.navbar-toggler type="button" data-toggle="collapse" data-target="#menu" aria-controls="menu" aria-expanded="false" aria-label="Toggle navigation"
      - if current_user
        = image_tag current_user.avatar_url(:small), class: "avatar"
      - else
        i.fa.fa-stream

    #menu.collapse.navbar-collapse
      input.search.search-bar placeholder="Search coming soon"

      ul.subnav.ml-auto
        - if user_signed_in?
          li.nav-item
            = link_to 'Write', write_path, class: "nav-link mr-3"
          li.nav-item
            = link_to 'Upload a PDF', new_upload_path, class: "nav-link mr-3"

          li.nav-item.dropdown
            a.dropdown-toggle.avatar-link href="#" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
              = image_tag current_user.avatar_url(:small), class: "avatar"

            .dropdown-menu.dropdown-menu-right
              a.dropdown-item href=dashboard_path
                | Your Dashboard
              a.dropdown-item href=user_path(current_user)
                | Your Profile
              a.dropdown-item href=edit_user_path(current_user)
                | Your Settings
              hr
              a.dropdown-item href="#"
                | 🙋🏻‍♀️
              hr
              = link_to 'Log Out', destroy_user_session_path, method: :delete, class:"dropdown-item"

        - else
          li.nav-item
            = link_to 'Log In', new_user_session_path, class: "nav-link mr-3"


